#include <stdio.h>

/**
 * main - prints its name, followed by a new line.
 * @argc: number of command line arguments/arg. typed
 * @argv: array containing the program line arg
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
